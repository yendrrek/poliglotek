import { TranslationFormResponsiveDirective } from './translation-form-responsive.directive';

describe('InputFontResponsiveDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new TranslationFormResponsiveDirective();
    expect(directive).toBeTruthy();
  });
});
