import { MenuResponsiveDirective } from './menu-responsive.directive';

describe('ResponsiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuResponsiveDirective();
    expect(directive).toBeTruthy();
  });
});
