import { AboutResponsiveDirective } from './about-responsive.directive';

describe('AboutResponsiveDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new AboutResponsiveDirective();
    expect(directive).toBeTruthy();
  });
});
