import { FormLabelResponsiveDirective } from './form-label-responsive.directive';

describe('InputFontResponsiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FormLabelResponsiveDirective();
    expect(directive).toBeTruthy();
  });
});
