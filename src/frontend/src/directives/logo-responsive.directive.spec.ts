import { LogoResponsiveDirective } from './logo-responsive.directive';

describe('LogoResponsiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoResponsiveDirective();
    expect(directive).toBeTruthy();
  });
});
